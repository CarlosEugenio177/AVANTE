version: '3.8'
services:
  db_test:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: avante_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
